<h1><%= @flat.name %></h1><div class="row">
  <div class="col-md-8">
    <h1><%= @flat.name %></h1>
    <p><%= @flat.address %></p>
    <p><%= @flat.description %></p>
    <p>Price per night: <%= @flat.price_per_night %></p>
    <p>Number of guests: <%= @flat.number_of_guests %></p>
    <% if @flat.picture_url.present? %>
      <img src="<%= @flat.picture_url %>" alt="<%= @flat.name %>" class="img-fluid mb-4">
    <% end %>
  </div>
  <div class="col-md-4">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Actions</h5>
        <%= link_to "Edit", edit_flat_path(@flat), class: "btn btn-warning btn-block mb-2" %>
        <%= link_to "Delete", flat_path(@flat), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger btn-block" %>
        <%= link_to "Back to list", flats_path, class: "btn btn-secondary btn-block mt-2" %>
      </div>
    </div>
  </div>
</div>

<p><%= @flat.address %></p>
<p><%= @flat.description %></p>
<p>Price per night: <%= @flat.price_per_night %></p>
<p>Number of guests: <%= @flat.number_of_guests %></p>
<% if @flat.picture_url.present? %>
  <img src="<%= @flat.picture_url %>" alt="<%= @flat.name %>" class="img-fluid">
<% end %>

<%= link_to "Edit", edit_flat_path(@flat), class: "btn btn-primary" %>
<%= link_to "Delete", flat_path(@flat), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
<%= link_to "Back to list", flats_path, class: "btn btn-secondary" %>
